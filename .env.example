# ExamRigor Environment Configuration
# This app is fully client-side and does NOT require any API keys.
# All data is stored in the browser's localStorage.
#
# No environment variables are needed to run this app.
# Simply run: npm install && npm run dev
